function applyExtraSetup(sequelize) {
	// const { client, address, transaction, balance } = sequelize.models;
	
	// Create tx/txinfo association
	// client.hasMany(address);
	// address.belongsTo(client);

}

module.exports = { applyExtraSetup };